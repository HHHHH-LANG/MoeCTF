# Linux虚拟环境安装和基础命令（难 一直在踩坑）
## 解题步骤
- 第一步 阅读下方有关的基础命令 点击启动 复制到wsrx中得到端口号
- 第二步 打开cmd 使用ssh连接环境"ssh 用户名@IP -p 端口号"
- 第三步 输入密码(注意仍然是“隐形的”)进入linux的虚拟环境中
- 第四步 尝试题目中的各种命令（我觉得非常难 刚开始没有任何效果 一直在碰壁 我尝试用```ls-la cat```等查看文件 没有发现任何flag有关的东西）
- 第五步 尝试用"su -"转换视角至root用户查看文件 发现没有flag（我重新阅读了题目 发现题目中给了超级管理员的密码 再结合下面的命令中给到了su的用法 于是我尝试了切换身份 重新读取文件发现依然没有发现flag 后面我重新退回student用户身份 再次使用```ls-la```读取 发现了flag文件）
- 第六步 输入exit退回至stdent用户 用"cd ~"回到家目录 再次使用ls -la读取文件 再其中发现了flag文件
### 注意 提醒事项
一 第一次用```ls -la```查看文件只发现了chmod.sh 和env.py误以为flag在里面 尝试了多次环境变量测试 **不要被诱饵文件带偏**
二 应该仔细阅读题目中的有效信息 但是应该注意辨别 如发现题目中给了超级管理员的密码时在root身份下再次查看文件 依然不在 **不要迷信root权限**
- 三 以后可以先尝试"cd ~"查看家目录 这样可以更快速的获取信息 **不要一直卡在某个思维定局里**
![alt text](image-4.png)
### 总结 
虽然花费了将近一个小时绕弯子 不过让我真正地了解了linux的目录 还是很值的